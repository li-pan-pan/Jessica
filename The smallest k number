class Solution {
public:
    vector<int> getLeastNumbers(vector<int>& arr, int k) {
        sort(arr.begin(),arr.end());
        vector<int>Newarr;
        for(int i=0;i<k;++i)
        {
            Newarr.push_back(arr[i]);
        }

    return Newarr;
    }
};
