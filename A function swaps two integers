#include<stdio.h>
void swap(int *x, int *y)
{
	int temp;
	temp = *x;
	*x = *y;
	*y = temp;
}
int main()
{
	int x, y;
	printf("请输入需要交换的两个数\n");
	scanf("%d %d", &x, &y);
	swap(&x, &y);
	printf("x=%d,y=%d", x, y);
	return 0;
}
