#include<stdio.h>
int main()
{
    char a;
    scanf("%c",&a);
    for(int n=1;n<=5;++n)
    {
        for(int i=5-n;i>0;--i)
        {
            printf(" ");
        }
        for(int j=1;j<=n;++j)
        {
            printf("%c ",a);
        }
        printf("\n");
    }
    return 0;
}
